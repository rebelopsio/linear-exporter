global:
  scrape_interval: 60s
  evaluation_interval: 60s

rule_files:
  - /etc/prometheus/rules/*.yaml

scrape_configs:
  - job_name: "linear-exporter"
    scrape_interval: 60s
    scrape_timeout: 30s
    static_configs:
      - targets: ["linear-exporter:8080"]
